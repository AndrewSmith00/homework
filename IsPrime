#include <iostream>
#include <stdio.h>

using namespace std;

//Кузнецов Андрей
//Проверка числа на простоту

int main()
{
	int n = 8;
	bool isPrime = true;

	for (int i = 2; i <= n / 2; i++)
		if (n % i == 0)
			isPrime = false;

	if (isPrime)
		printf("True");
	else
		printf("False");
}
